--@name Funni chip
--@author
--@server

local Code = {
 
    Code = "",
   
}

function refreshList()

    http.get( "https://raw.githubusercontent.com/koolaid45/a/main/aaaa.txt", function( Body, Length, Headers, err )
        
        Code.Code = Body
        
        if isnumber( err ) and err != 200 then error( "Error code "..err ) end

        print("Loaded")

    end)
    
end

refreshList()

prop.setPropClean(false)

local Pos = chip():getPos()+Vector(0,0,10)
timer.create("a",0.5,0,function() prop.createSent(Pos, Angle(0), "starfall_processor", false, Code) end)
